# Environment
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
uv.lock.tmp

# Node / frontend
node_modules/
dist/
.vite/
coverage/

# Editors / OS
.vscode/
.idea/
.DS_Store

# Local tooling caches
.atl/
.codegraph/

# github-checks-publisher PR7 — operator-created, worker-only bind mount for
# the GitHub App private key PEM (docker-compose.yml). Never a real secret
# committed here; the directory is created locally, not tracked.
secrets/
